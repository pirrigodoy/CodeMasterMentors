import { TeacherFavouriteList } from './teacher-favourite-list.model';

describe('TeacherFavouriteList', () => {
  it('should create an instance', () => {
    expect(new TeacherFavouriteList()).toBeTruthy();
  });
});
