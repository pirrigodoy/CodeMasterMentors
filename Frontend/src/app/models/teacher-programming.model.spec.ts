import { TeacherProgramming } from './teacher-programming.model';

describe('TeacherProgramming', () => {
  it('should create an instance', () => {
    expect(new TeacherProgramming()).toBeTruthy();
  });
});
