import { ProgrammingLaguage } from './programming-laguage.model';

describe('ProgrammingLaguage', () => {
  it('should create an instance', () => {
    expect(new ProgrammingLaguage()).toBeTruthy();
  });
});
